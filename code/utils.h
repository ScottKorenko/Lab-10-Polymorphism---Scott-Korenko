#ifndef UTILS_H
#define UTILS_H
#include "string"
#include "exception"
using namespace std;

/**
 * Checks if s1 and s2 have the same content
 * https://cplusplus.com/reference/string/string/compare/
 * @param s1
 * @param s2
 * @return 0 if same
 */
int notEquals(const string& s1, const string& s2);



#endif //UTILS_H
